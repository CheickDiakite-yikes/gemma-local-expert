# Data

This directory stores local runtime data for the scaffold.

Expected contents:

- `migrations/` for schema files
- `field-assistant.db` for the local SQLite database during development

Database files are ignored by Git.
