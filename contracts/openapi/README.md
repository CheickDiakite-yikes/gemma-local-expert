# OpenAPI Snapshot

The generated engine schema should be written here as:

- `field-assistant.openapi.json`

Generate it with:

```bash
uv run python scripts/export_openapi.py
```

Do not hand-edit the generated JSON.
